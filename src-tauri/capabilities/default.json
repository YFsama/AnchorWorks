{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Anchorworks — grants the webview access to dialog/fs/shell APIs used by the editor.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "shell:default",
    "deep-link:default",
    "updater:default"
  ]
}
